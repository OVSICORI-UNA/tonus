#!/usr/bin/env python

# Python Standard Library

# Other dependencies
import logging

# Local files
from tonus.gui.root import Root


if __name__ == '__main__':
    logging.basicConfig(level=logging.INFO)

    Root().mainloop()
